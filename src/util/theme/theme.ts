import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: "Satoshi-Variable, sans-serif",
    body: "Switzer-Variable, sans-serif",
  },
});

export default theme;
